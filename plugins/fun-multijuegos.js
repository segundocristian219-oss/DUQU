let handler = async (m, { conn, command, text, usedPrefix }) => {
if (!text) throw `${lenguajeGB['smsAvisoMG']()}𝙀𝙏𝙄𝙌𝙐𝙀𝙏𝙀 @𝙏𝘼𝙂 𝙊 𝙀𝙎𝘾𝙍𝙄𝘽𝘼 𝙀𝙇 𝙉𝙊𝙈𝘽𝙍𝙀\n𝙏𝘼𝙂 𝙎𝙊𝙈𝙀𝙊𝙉𝙀 @𝙏𝘼𝙂 𝙊𝙍 𝙏𝙔𝙋𝙀 𝙏𝙃𝙀 𝙉𝘼𝙈𝙀`

// ------------------ EXISTENTES ------------------------------------------------------------------------------

if (command == 'cachuda') {
let juego = `_*${text.toUpperCase()}* *ES/IS* *${(100).getRandom()}%* *GAY*_ 🏳️‍🌈`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'adoptada') {
let juego = `_*${text.toUpperCase()}* *ES/IS* *${(100).getRandom()}%* *${command.replace('how', '').toUpperCase()}*_ sus padres se fueron x pañales 😞😂`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'cachudo') {
let juego = `_*${text.toUpperCase()}* *ES/IS* *${(100).getRandom()}%* *${command.replace('how', '').toUpperCase()}*_ 😂😂`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'adoptado') {
let juego = `_*${text.toUpperCase()}* *ES/IS* *${(100).getRandom()}%* *${command.replace('how', '').toUpperCase()}*_ Sus padres se fueron x pañales 😞😂`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'sinpito') {
let juego = `_*${text.toUpperCase()}* *ES/IS* *${(100).getRandom()}%* *${command.replace('how', '').toUpperCase()},* *ASI CREE QUE LA TIENE GRANDE? 😂 XD*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'sinpoto') {
let juego = `_*${text.toUpperCase()}* *ES/IS* *${(100).getRandom()}%* *${command.replace('how', '').toUpperCase()},* *NO TIENE NI POTO MAS PLANA 😂XD*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'sintetas') {
let juego = `_*${text.toUpperCase()}* *ES/IS* *${(100).getRandom()}%* *${command.replace('how', '').toUpperCase()},* *NO TIENE NI TETAS Y SE CREE TETONA? 😂 XD*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'feo') {
let juego = `_*${text.toUpperCase()}* *ES* *${(100).getRandom()}%* *${command.replace('how', '').toUpperCase()},* *MEJOR MÁTATE HERMANO 🤢*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'fea') {
let juego = `_*${text.toUpperCase()}* *ES* *${(100).getRandom()}%* *${command.replace('how', '').toUpperCase()} ASI SE CREÍA HERMOSA? 😂*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'negro') {
let juego = `_*${text.toUpperCase()}* *ES* *${(100).getRandom()}%* *${command.replace('how', '').toUpperCase()} 👨🏾‍🦱, MAS NEGRO QUE SU POTO? 😂*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'negra') {
let juego = `_*${text.toUpperCase()}* *ES* *${(100).getRandom()}%* *${command.replace('how', '').toUpperCase()} 👱🏾‍♀️, MAS NEGRA QUE SU CUCA? 😂*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'love2') {
let juego = `*❤️❤️ MEDIDOR DE AMOR ❤️❤️*\n*_El amor de ${text.toUpperCase()} ES DE ${(100).getRandom()}% Deberias pedirle que sea tu  novia/o ?_*`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

// ------------------ NUEVOS (SIN MEME) ----------------------------------------------------------------------

if (command == 'casar') {
let juego = `_*${text.toUpperCase()}* *SE CASA CON* *${(100).getRandom()}%* *DE SU CRUSH 💍*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'divorcio') {
let juego = `_*${text.toUpperCase()}* *TIENE UN* *${(100).getRandom()}%* *DE PROBABILIDAD DE DIVORCIARSE 💔😂*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'enana') {
let juego = `_*${text.toUpperCase()}* *ES* *${(100).getRandom()}%* *ENANA 😂🧚‍♀️*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'enano') {
let juego = `_*${text.toUpperCase()}* *ES* *${(100).getRandom()}%* *ENANO 😂🧚‍♂️*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'gay') {
let juego = `_*${text.toUpperCase()}* *ES* *${(100).getRandom()}%* *GAY 🏳️‍🌈😂*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'juegos') {
let juego = `_*${text.toUpperCase()}* *TIENE* *${(100).getRandom()}%* *DE SUERTE EN LOS JUEGOS 🎮😂*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'lesbiana') {
let juego = `_*${text.toUpperCase()}* *ES* *${(100).getRandom()}%* *LESBIANA 🌈🤭*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'manca') {
let juego = `_*${text.toUpperCase()}* *ES* *${(100).getRandom()}%* *MANCA 😭😂*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'manco') {
let juego = `_*${text.toUpperCase()}* *ES* *${(100).getRandom()}%* *MANCO 😭😂*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'matrimonios') {
let juego = `_*${text.toUpperCase()}* *TIENE* *${(100).getRandom()}%* *DE PROBABILIDAD DE TENER MATRIMONIOS 💍😂*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'pajera') {
let juego = `_*${text.toUpperCase()}* *ES* *${(100).getRandom()}%* *PAJERA 😳✋😂*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'pajero') {
let juego = `_*${text.toUpperCase()}* *ES* *${(100).getRandom()}%* *PAJERO 😳✋😂*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'personalidad') {
let juego = `_*${text.toUpperCase()}* *TIENE* *${(100).getRandom()}%* *DE PERSONALIDAD 🔥😎*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'peruana') {
let juego = `_*${text.toUpperCase()}* *ES* *${(100).getRandom()}%* *PERUANA 🇵🇪😂*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'peruano') {
let juego = `_*${text.toUpperCase()}* *ES* *${(100).getRandom()}%* *PERUANO 🇵🇪😂*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'poema') {
let juego = `_*${text.toUpperCase()}* *TIENE* *${(100).getRandom()}%* *DE SER POETA 📜💕*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'ppt') {
let juego = `_*${text.toUpperCase()}* *TIENE* *${(100).getRandom()}%* *DE SER PRO EN PIEDRA PAPEL TIJERA ✂️🪨📄*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'puto') {
let juego = `_*${text.toUpperCase()}* *ES* *${(100).getRandom()}%* *PUTO 😂🔥*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'rata') {
let juego = `_*${text.toUpperCase()}* *ES* *${(100).getRandom()}%* *RATA 🐀😂*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

if (command == 'pegar') {
let juego = `_*${text.toUpperCase()}* *TE PEGA CON UN* *${(100).getRandom()}%* *DE FUERZA 🥊😂*_`.trim()
await conn.reply(m.chat, juego, m, m.mentionedJid ? { mentions: m.mentionedJid } : {})}

}

handler.help = [
'lov2','cachuda','negra','adoptado','sintetas','sinpoto','sinpito','feo','cachudo','fea','negro','adoptada',
'casar','divorcio','enana','enano','gay','juegos','lesbiana','manca','manco','matrimonios','pajera','pajero',
'personalidad','peruana','peruano','poema','ppt','puto','rata','pegar'
].map(v => v + ' @tag | nombre')

handler.tags = ['fun']

handler.command = /^love2|cachuda|adoptado|adoptada|sintetas|sinpoto|sinpito|feo|fea|cachudo|negro|negra|casar|divorcio|enana|enano|gay|juegos|lesbiana|manca|manco|matrimonios|pajera|pajero|personalidad|peruana|peruano|poema|ppt|puto|rata|pegar/i

handler.exp = 100

export default handler